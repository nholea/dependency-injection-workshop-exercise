package app.secondary;

public class LetterSender {
    public void send(String address, String content) {
        System.out
            .println("A letter will be printed and sent to " + address + " with the following content: " + content);
    }
}
